[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18.18.2"
  NPM_FLAGS = "--version"

[dev]
  command = "npm run dev"
  port = 5173
  publish = "dist"
  targetPort = 5173
